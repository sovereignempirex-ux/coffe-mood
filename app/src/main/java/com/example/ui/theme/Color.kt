package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Immersive UI Palette
val MoodDarkInk = Color(0xFF050402) // Pure Obsidian Noir
val MoodDarkEspresso = Color(0xFF0D0B08)
val MoodDarkCard = Color(0xFF14100C)
val MoodDarkCardElevated = Color(0xFF1C1712)

// Warm Amber & Gold Accents
val MoodAmberPrimary = Color(0xFFF59E0B) // Amber 500
val MoodAmberDark = Color(0xFFD97706)    // Amber 600
val MoodAmberDeep = Color(0xFF78350F)    // Amber 900
val MoodAmberGlow = Color(0xFFFBBF24)    // Amber 400

val MoodGoldPrimary = Color(0xFFF59E0B)
val MoodGoldSecondary = Color(0xFFD97706)
val MoodGoldDark = Color(0xFFB45309)

// Translucent Glassmorphism (white/5, white/10, white/20)
val MoodGlassSurface = Color(0x0DFFFFFF) // 5% White Glass
val MoodGlassSurfaceElevated = Color(0x14FFFFFF) // 8% White Glass
val MoodGlassBorder = Color(0x1AFFFFFF) // 10% White Border
val MoodGlassHighlight = Color(0x33FFFFFF) // 20% White Highlight

// Secondary Accents
val MoodTealNeon = Color(0xFF38BDF8)
val MoodTealGlow = Color(0xFF7DD3FC)
val MoodBerryRose = Color(0xFFEF4444)

// Clean High-Contrast Slate Typography
val MoodCreamText = Color(0xFFF8FAFC) // Slate-50 High Contrast Text
val MoodMutedText = Color(0xFF94A3B8) // Slate-400
val MoodSubtleText = Color(0xFF64748B) // Slate-500


